'use strict';

describe('MessageAlertStore', function() {
  var store;

  beforeEach(function() {
    store = require('stores/MessageAlertStore.js');
  });

  it('should be defined', function() {
    expect(store).toBeDefined();
  });
});
